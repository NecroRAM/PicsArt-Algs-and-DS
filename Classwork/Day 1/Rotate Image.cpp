#include <iostream>
#include <vector>

using mat = std::vector<std::vector<int>>;

int main()
{
	mat v(3);
	for (int i = 0; i < 3; ++i)
	{
		std::vector<int> v();
		for (int j = 0; j < 3; ++j)
		{
			v.push_back();
		}

	}
}